{
  "name": "adaptation_agent",
  "version": "1.0.0",
  "description": "Adaptation Agent for UI based on user emotions",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc",
    "start": "npx tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "@types/node": "^18.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@types/seedrandom": "^3.0.2",
    "seedrandom": "^3.0.5"
  }
}
